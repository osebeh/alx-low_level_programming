#include <stdio.h>

/*
 * main() - this is the program's entry point
 * Program prints all the numbers of base 16 in lower case
 * all the code is in the main function
 * only putchar is used
 * /
int main(void)//main() - this is the program's entry point
{
int a;

for (a = 0; a < 10; a++)

putchar(a + '0');

for (a = 0; a < 6; a++)

putchar(a + 'a');

putchar('\n');

return (0);

}
